[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Minimal output by default
addopts = -q
# Custom markers
markers =
    qt_integration: marks tests as requiring Qt/GUI (may be skipped in headless CI)
