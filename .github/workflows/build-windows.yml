name: Build (Windows)

on:
  push:
    branches: [ main ]
    tags:
      - "v*"
  pull_request:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install deps
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          pip install pyinstaller

      - name: Add repo to PYTHONPATH
        shell: pwsh
        run: |
          "PYTHONPATH=$env:GITHUB_WORKSPACE" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Run tests
        shell: pwsh
        run: |
          python -m pytest -v

      - name: Build exe (PyInstaller)
        shell: pwsh
        run: |
          pyinstaller ReceiptDesigner.spec

      - name: Upload artifact (exe)
        uses: actions/upload-artifact@v4
        with:
          name: ReceiptDesigner-windows
          path: dist/ReceiptDesigner.exe

      - name: Create GitHub Release and upload exe (tags only)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          name: Receipt Designer ${{ github.ref_name }}
          files: dist/ReceiptDesigner.exe
          fail_on_unmatched_files: true
