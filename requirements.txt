# Receipt Designer - Runtime Dependencies
# Install: pip install -r requirements.txt

# ── Core (required) ──────────────────────────────────────────────
PySide6>=6.5

# ── Barcode / QR generation ─────────────────────────────────────
Pillow>=9.0
python-barcode>=0.15
qrcode>=7.0

# ── Printing ─────────────────────────────────────────────────────
pyserial>=3.5              # Serial printers
pyusb>=1.2                 # USB printers
python-escpos>=3.0         # ESC/POS thermal printers

# ── Advanced 2D barcodes (optional; also needs Ghostscript on PATH) ──
# treepoem>=3.0
